apiVersion: ingress.uniphar.ie/v1
kind: AbstractIngress
metadata:
  name: {{ .Chart.Name }}
spec:
  isPrivate: true
  serviceName: {{ .Chart.Name }}-srv
  servicePort: 80
  urlPrefix: {{ .Values.appPath }}