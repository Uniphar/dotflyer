apiVersion: devops.uniphar.ie/v1
kind: PlatformDataPilotAdxDatabase
metadata:
  name: DotFlyerEmails
  namespace: platform-app
spec:
  # 8 years
  softDeleteRetentionDays: 2920 
  hotCachePeriodDays: 1
  access:
    workloadIdentity:
      name: compute-aks-platform-app-dev-k8s-sa
      namespace: platform-app
